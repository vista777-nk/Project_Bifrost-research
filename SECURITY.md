# SECURITY.md — 安全策略

> **Bifrost 是一个工业软件中继适配层。安全不是可选项，是设计约束。**

---

## 支持版本

| 版本 | 支持状态 |
|------|:---:|
| `0.x` (开发中) | ⚠️ 安全补丁视情况提供 |

正式发布后将启用语义化版本安全支持策略。

---

## 报告漏洞

如果你发现安全漏洞，**请不要在公开 Issue 中报告**。请通过以下方式私下报告：

📧 **Email**：[待填写]

请在你的报告中包含：

1. 漏洞的详细描述
2. 复现步骤
3. 受影响的版本
4. 可能的修复建议（如有）

我们将在 **48 小时内**确认收到你的报告，并在 **7 天内**提供初步评估。安全修复将优先处理，并在修复后就绪后协调公开披露。

---

## 安全设计原则

Bifrost 在设计阶段就考虑了以下安全约束：

### 1. 破坏性操作必须显式确认

以下操作**绝不**自动执行：

- 覆盖已有工程文件
- 删除任何文件
- 写入硬件（烧录 Flash / EEPROM）
- 擦除芯片存储
- 批量导出覆盖旧结果
- 发送生产命令

这些操作通过 `confirm_action` 工具实现人机确认环。

### 2. Dry-Run 优先

所有操作支持 `mode: "dry_run"`，在真实执行前预览效果。

### 3. 最小权限原则

- MCP Server 只暴露必要的工具
- Adapter 只调用工业软件的必要接口
- 不保留任何工业软件的敏感凭证（密钥、许可证等）

### 4. 日志与审计

- 所有操作记录：时间戳、动作名、参数摘要、结果、产物清单
- 日志不包含敏感信息（密码、令牌等自动脱敏）
- 支持日志导出用于审计

### 5. 依赖安全

- Python 依赖固定版本（`requirements.txt` 含精确版本号）
- 定期审查依赖漏洞（`pip-audit`）
- Core 层零外部依赖

---

## 已知风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 工业软件 CLI 注入 | 恶意参数传递给 CLI | 严格参数校验 + 白名单 |
| 固件文件篡改 | 烧入恶意固件 | 烧录前校验 SHA256 |
| MCP Server 未授权访问 | 任意调用工业软件 | MCP 协议层的认证（未来） |
| 日志泄露 | 工程文件路径等敏感信息暴露 | 日志脱敏 |

---

## 安全相关配置

```python
# core/errors.py — 高风险动作的错误标记
class ConfirmationRequiredError(RelayError):
    """需要用户确认才能继续"""
    severity = ErrorSeverity.NEEDS_HUMAN
    recoverable = True
```

---

## 致谢

感谢所有通过私下报告帮助改进 Bifrost 安全性的研究人员和用户。我们将在修复发布后（征得报告者同意）在此列出致谢名单。
