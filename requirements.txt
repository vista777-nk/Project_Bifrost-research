# Bifrost — Python 依赖
# 最低 Python 版本：3.11

# === 核心依赖（core/ — 框架无关，Codex + Hermes 共享） ===
pydantic>=2.10.0,<3.0.0       # 数据模型与校验
structlog>=24.4.0,<26.0.0      # 结构化日志

sexpdata==1.0.2
lxml>=5.0.0,<7.0.0
networkx>=3.2,<4.0

# === Codex 插件（codex-relay/） ===
mcp>=2.0.0,<3.0.0              # MCP Server (v2 — 2026-07-28 发布，API 重写)
typer>=0.15.0,<1.0.0           # CLI 入口

# === 适配器依赖（按需安装） ===
pywin32>=308; sys_platform == "win32"   # AutoCAD + SolidWorks COM 接口
# pyocd>=0.36.0,<1.0.0                 # ARM Cortex 调试（备选，主路径为 Keil/CubeIDE CLI）

# === 开发依赖 ===
# pytest, pytest-mock, pytest-asyncio, pytest-cov, mypy, black, ruff
# 统一以 pyproject.toml [project.optional-dependencies] dev 为唯一来源：
#   pip install -e ".[dev]"
