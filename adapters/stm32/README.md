# adapters/stm32 — 占位目录（不开发）

> 本目录仅为向后兼容保留的**占位**，不包含任何实现，也不投入开发。

STM32 烧录/调试能力自 Phase 2 起统一由 [`adapters/stm32cubeide/`](../stm32cubeide/) 提供
（STM32CubeProgrammer CLI / pyocd 路径，详见 `docs/research-stm32-cli.md`）。

对应 Skill：`stm32-flash`（向后兼容，推荐使用 `cubeide-build`）。
